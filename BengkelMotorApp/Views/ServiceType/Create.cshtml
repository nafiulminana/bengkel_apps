@model BengkelMotorApp.ViewModel.ServiceTypeViewModel

@{
    ViewData["Title"] = "Create Service Type";
}

<h1>Create Service Type</h1>
<div class="col-md-12">
    <!-- general form elements -->
    <div class="card card-default">
        @using (Html.BeginForm("Create", "ServiceType", FormMethod.Post, new { @id = "quickform" }))
        {
            <div class="card-body">

                <div class="form-group">
                    <label for="Name">Name</label>
                    <input type="text" class="form-control" id="Name" name="Name" placeholder="Name" maxlength="200" required>
                </div>
                <div class="form-group">
                    <label for="Price">Price</label>
                    <input type="number" class="form-control" id="Price" name="Price" placeholder="Price">
                </div>
            </div>
            <!-- /.card-body -->

            <div class="card-footer">
                <button type="submit" class="btn btn-primary">Submit</button>
                <a href="@Url.Action("Index","ServiceType")" class="btn btn-danger">Back</a>
            </div>
        }
    </div>
    <!-- /.card -->
</div>